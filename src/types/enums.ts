export enum MessageType {
    TEXT = "TEXT",
    SINGLE_PHOTO = "SINGLE_PHOTO",
    MEDIA_GROUP = "MEDIA_GROUP",
    WEB_LINK = "WEB_LINK",
    UNKNOWN = "UNKNOWN",
}

export enum ProcessingMode {
    OPTIMIZE = "OPTIMIZE",
    PROCESS = "PROCESS",
}
